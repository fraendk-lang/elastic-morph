# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Node (only used for tests/tooling)
node_modules/
npm-debug.log*

# Vercel
.vercel

# Playwright / E2E
test-results/
playwright-report/
blob-report/

# Local exports / scratch (landing demo reel is tracked)
*.mp4
!assets/elasticmorph_elasticfield_dustreel.mp4
*.source.mp4
*.webm
elastic-morph-*.mp4

# Local music / artwork drafts (not part of the app repo)
Elastic Field Music/
Neue Cover/
Neues Cover.png

# Brainstorming visual-companion session files (superpowers plugin)
.superpowers/

# Local editor/tool scratch, not part of the app repo (has been swept in by
# broad `git add -A`/`git add .` sessions before -- keep it explicitly ignored)
.composer-pull/
