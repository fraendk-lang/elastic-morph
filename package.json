{
  "name": "elastic-morph",
  "version": "94.0.0",
  "private": true,
  "description": "Elastic Morph — browser audio-visual app",
  "scripts": {
    "build": "node build.js",
    "test": "node test.js",
    "start": "npx --yes serve -l 3456",
    "test:e2e": "playwright test",
    "check:demo": "node scripts/check-demo.js",
    "ci": "node build.js && node test.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
